# Test Environment Variables
# These are used during testing and should be different from production

# JWT Secret for testing
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production

# Database (use separate test database)
DATABASE_URL=postgresql://test:test@localhost:5432/mindpal_test

# AI API Keys (mock or test keys)
GEMINI_API_KEY=test-gemini-api-key
OPENAI_API_KEY=test-openai-api-key
ANTHROPIC_API_KEY=test-anthropic-api-key

# App Configuration
NODE_ENV=test
PORT=3001
CORS_ORIGIN=http://localhost:3000
